name: Deploy para EC2 Debian 13

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Configurar chave SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Testar conexão SSH
        run: ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no jocerlan@${{ secrets.EC2_HOST }} "echo ✅ Conexão OK!"

      - name: Deploy no servidor Debian 13
        run: |
          ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=yes jocerlan@${{ secrets.EC2_HOST }} "
            set -e
            cd /home/jocerlan || exit

            if [ ! -d meu-portifolio ]; then
              echo '📥 Clonando projeto pela primeira vez...'
              git clone https://github.com/SamaraBraga/Samara_Braga.portifolio.git meu-portifolio
            else
              if [ ! -d meu-portifolio/.git ]; then
                echo '⚠️ Pasta existe mas não é repositório Git, removendo...'
                rm -rf meu-portifolio
                git clone https://github.com/SamaraBraga/Samara_Braga.portifolio.git meu-portifolio
              else
                echo '🔄 Atualizando repositório existente...'
                cd meu-portifolio
                git fetch origin main
                git reset --hard origin/main
                cd ..
              fi
            fi

            cd meu-portifolio

            echo '🐳 Recriando container Docker...'
            sudo docker stop site-samara || true
            sudo docker rm site-samara || true
            sudo docker build -t site-samara .
            sudo docker run -d --restart always -p 8080:80 --name site-samara site-samara
          "